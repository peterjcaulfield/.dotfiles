#load other dotfiles
[[ ~/.bashrc ]] && source ~/.bashrc
[[ ~/.profile ]] && source ~/.profile
[[ ~/.bash_aliases ]] && source ~/.bash_aliases
[[ ~/.local_bash_aliases ]] && source ~/.local_bash_aliases
[[ ~/git-completion.bash ]] && source ~/git-completion.bash

set -o vi
export EDITOR=vim
